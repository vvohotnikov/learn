const — всё таки можно изменить через obj например
let — переменная
var — старый, виден везде и даже до обявления
массив - частный тип объекта
все ответы от пользователя - строка
continue — пропустить шаг
Создавая функцию не неадо точку с запятой,
Закрывая что-то - надо

str
slice — вхождение (начало, конец)
substring — вхождение (начало, конец)
substr (начало, длина)

Динамическая типизация
String() - old
норм - сложение со строкой даёт строку (5 + '');
Number() - old
норм - унарный плюс (+'5');
parseInt — перевод текста в инт
parseFloat — перевод в десятичную
0, '', null, undefined, NaN — false, всё остальное true 
!!'что-то' -bool

Object.keys(obj) — выводит в массиве все ключи объекта
Детруктуризация — const { border, bg } = option.colors(они вот тут были);
присваивание объекта к объекту — передача по ссылке, а не создание копии
копию можно создать перебором(но глубокие объекты ссылаются, если их не обработать) — поверхностное копирование
Object.assign(куда, что) — вложить. Также можно создать поверхностную копию, только "куда" = {}.
Object.setPrototypeOf(кому, от кого); — наследование
const кому =  Object.create(от кого); наследование


arr
перебор в массиве/строке for (let i of arr) {}; — если нужно остановить или пропустить
letngt — последний индекс +1 (могут быть пустые ячейки внтури)
forEach(function(item, index, arr){
	функция;
});
.push — добавить
.map модификация 
.сорт работает как сорт СТРОК, даже для интов
чтобы отсортить инты — нужно добавить в метод сорт(тут) —function солбек(a, b) {
return a-b;
}
присвоение Arr = также ссылка
newArr = oldArr.slice(); — копия
... — spread (разворот массива на элементы), все элементы массива вносит куда то по отдельности. А также можно делать поверхностную копию (объектов и массивов).


document.getElemmentBy...('') — псевдокласс
document.createElement('')
document.querySelectorAll()псведомассив
document.querySelector('.obg')

defer — подгружает скрипт в фоне и ждёт ДОМ
async — подгружается и сразу запускается

.classList (для работы с классами объекта)
.add('red','blue')
.remove
.toggle — (преключать вкл/выкл)
.contains('red') — есть ли класс такой


делегирование
наследование свойств от родителя к чайлд 

addEventListener('click', (event) => {
if (event.target && event.target.tagName...)
});

target.matches('button.red')

имя = setTimeout(что, время);

clearInterval(имя);

const now = new Date();

размеры:
clientWitdth/Height
offsetWitdth/Height
scrollWitdth/Height
scrollTop — показывает текущее значение
.getBoundingClientRect().top/etx — получить все рзмеры эл-та
document.documentElement.scrollTop — промотка от нчала страницы

Показать стили:
window.getComputedStyl